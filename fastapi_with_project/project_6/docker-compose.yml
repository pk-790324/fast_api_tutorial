services:
  vector-db:
    image: qdrant/qdrant
    ports:
      - 6333:6333

# after that run : docker compose up -d 
# then run docker ps
# then open in "http://localhost:6333/dashboard" in browser to see qdrant